# Runtime data is created by the local app and may contain operational requests.
data/harper-store.json
data/*.tmp
data/
!data/harper-private-demo.json

# Build output. Regenerated by `npm run build`; never committed.
inbox-ui.bundle.js
inbox-bridge.bundle.js
render-local.log
render-local.err
*.log

# Dependencies and local editor files.
node_modules/
.env
.env.*
!.env.example
.DS_Store

# Local working copies created by another tool. Not part of the site.
*-Goku.*
